from infrastructure.manager_base import ManagerBase

class InstructionRoutingManager(ManagerBase):
    MANAGER_ID = "CAIN_INSTRUCTION_ROUTING"
    DEPARTMENT = "CAIN"
    DIVISION = "instruction_routing"
