from infrastructure.manager_base import ManagerBase

class InterruptAuthorityManager(ManagerBase):
    MANAGER_ID = "CERN_INTERRUPT_AUTHORITY"
    DEPARTMENT = "CERN"
    DIVISION = "interrupt_authority"
